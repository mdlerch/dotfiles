[user]
	name = Michael Lerch
	email = mdlerch@gmail.com
[core]
	editor = nvim
	excludesfile = /home/mike/.gitignore_global
[color]
	ui = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[github]
	user = mdlerch
	token = 7035c51d364e417dc09ec3150883e32b
[alias]
	lg = "log --graph --decorate --pretty=oneline --abbrev-commit --all"
	lol = "log --graph --decorate --pretty=format:'%C(yellow)%h%Creset %C(yellow)%d%Creset %s %Cgreen(%cd) %Creset' --abbrev-commit --date=local --all"
	tracked = "ls-files --error-unmatch"
	unadd = "reset HEAD"
	pushall = "push -u origin --all"
	dt = difftool
	vd = difftool
	diff = "diff --check"
    istracked = "ls-files --error-unmatch"
[diff]
	tool = vimdiff
[merge]
	tool = vimdiff
[push]
	default = simple
[gist]
	home = /home/mike/work/gists/

[difftool]
	prompt = false

# vim:ft=gitconfig
[include]
    path = ~/dotfiles/git/ghi.gitconfig
    path = ~/dotfiles/github.gitconfig
